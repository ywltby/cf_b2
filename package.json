{
  "private": true,
  "type": "module",
  "name": "cloudflare-b2",
  "version": "2.0.0",
  "description": "D1-backed short-link CDN gateway for private S3-compatible buckets via a Cloudflare Worker",
  "main": "index.js",
  "scripts": {
    "pretest": "node scripts/check-no-buffering.mjs && node scripts/check-download-filename.mjs",
    "test": "cross-env HTTP_PROXY= HTTPS_PROXY= http_proxy= https_proxy= NO_PROXY=* vitest run",
    "test:watch": "cross-env HTTP_PROXY= HTTPS_PROXY= http_proxy= https_proxy= NO_PROXY=* vitest",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "author": "Pat Patterson <pat@backblaze.com>",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.6.0",
    "cross-env": "^10.1.0",
    "prettier": "^3.7.4",
    "vitest": "~2.1.0",
    "wrangler": "^4.54.0"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  }
}
